import {makeStyles} from "project-elements";

export default makeStyles(theme => ({
  card: {
    marginBottom: 20,
  },
  grid: {
    flexGrow: 1
  },
  margin: {
    margin: theme.spacing(1),
  },
}))