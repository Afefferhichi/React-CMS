fixture`Template`
  .page`https://localhost:3000/`

fixture.afterEach(t => {
  // t.navigateTo('https://localhost:3000/logout')
})

let testCount = 0;
test(`${++testCount}. User can choose one template for edit`, async t => {

});
